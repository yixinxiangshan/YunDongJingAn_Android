<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.nowapp.ecdemo_plugin"
    android:sharedUserId="cn.nowapp.plugin"
    android:versionCode="1"
    android:versionName="0.9" xmlns:tools="http://schemas.android.com/tools">
    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="16" tools:ignore="OldTargetApi"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name=".PluginActivity">
            <intent-filter >
                <action android:name="cn.nowapp.ecdemo_plugin.PluginActivity"/>
            </intent-filter>
        </activity>
    </application>
</manifest>